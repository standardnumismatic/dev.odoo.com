<odoo>

    <record id="view_stock_location_form_inherits" model="ir.ui.view">
        <field name="name">stock.location.form.inherits</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='removal_strategy_id']" position="after">
                 <field name="grading_company_vaults"/>
            </xpath>
        </field>
    </record>

    <record id="view_stock_picking_form_inherit" model="ir.ui.view">
        <field name="name">stock.picking.form.inherit</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='move_ids_without_package']//tree/field[@name='product_id']" position="after">
                <field name="barcode" attrs="{'readonly': [('state', 'not in', ['assigned'])]}"/>

            </xpath>
        </field>
    </record>

</odoo>
